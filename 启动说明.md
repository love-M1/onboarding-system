# 新员工入职任务协同系统启动说明

## 1. 环境要求

- Windows 10 或 Windows 11
- JDK 21
- Maven 3.9 或更高版本
- Node.js 20 或更高版本
- npm 10 或更高版本
- MySQL 8.0

确认环境是否可用：

```powershell
java -version
mvn -version
node -v
npm -v
mysql --version
```

## 2. 解压并进入项目目录

将压缩包解压到任意目录，然后在 PowerShell 中进入解压后的目录：

```powershell
cd <解压后的项目目录>
```

如果 PowerShell 阻止执行脚本，可仅在当前窗口临时放行：

```powershell
Set-ExecutionPolicy -Scope Process Bypass
```

## 3. 初始化数据库

确保 MySQL 8.0 服务已经启动。默认连接参数为：

- 地址：`127.0.0.1`
- 端口：`3306`
- 用户：`root`
- 数据库：`onboarding_sys`

在 PowerShell 中执行：

```powershell
$env:DB_PASSWORD = "你的 MySQL 密码"
.\scripts\init-db.ps1
```

如果数据库不在默认地址或端口：

```powershell
.\scripts\init-db.ps1 `
  -DbHost 127.0.0.1 `
  -Port 3306 `
  -User root `
  -Password "你的 MySQL 密码" `
  -DbName onboarding_sys
```

注意：初始化脚本会删除并重建 `employee`、`task_template`、`emp_task`、`task_action`、`task_attachment`、`user_account`、`phone_verification`、`auth_session` 八张表，随后写入 5 条演示任务模板。需要保留现有数据时不要重复执行。

## 4. 启动后端

打开一个新的 PowerShell 窗口，进入项目目录：

```powershell
cd <解压后的项目目录>
$env:DB_PASSWORD = "你的 MySQL 密码"
.\scripts\run-backend.ps1
```

看到类似以下信息表示启动成功：

```text
Tomcat started on port 8080
Started Application
```

后端地址：`http://127.0.0.1:8080`

首次启动会自动创建 HR 演示账号：

- 手机号：`13800000000`
- 密码：`Admin@123`

可以通过以下环境变量覆盖：

```powershell
$env:AUTH_HR_PHONE = "13800000000"
$env:AUTH_HR_PASSWORD = "你的管理员密码"
$env:AUTH_HR_NAME = "人事管理员"
```

任务附件默认保存在项目下的 `uploads/onboarding`，可通过以下环境变量改为其他非公开目录：

```powershell
$env:ONBOARDING_UPLOAD_DIR = "D:\onboarding-data\uploads"
```

如果 MySQL 地址或端口不同：

```powershell
.\scripts\run-backend.ps1 `
  -DbHost 127.0.0.1 `
  -DbPort 3306 `
  -DbName onboarding_sys `
  -DbUsername root `
  -DbPassword "你的 MySQL 密码"
```

## 5. 启动前端

再打开一个新的 PowerShell 窗口，进入项目目录：

```powershell
cd <解压后的项目目录>
.\scripts\run-frontend.ps1
```

首次启动时，脚本会自动执行 `npm install`。看到以下地址表示启动成功：

```text
http://127.0.0.1:5173
```

浏览器访问：

```text
http://127.0.0.1:5173
```

Vite 会把页面中的 `/api` 请求代理到 `http://127.0.0.1:8080`。

## 6. 注册和登录

系统使用手机号（工号）和密码登录：

- 人事管理员：维护任务模板和部门责任人，登记员工档案，查看进度汇总，归档和删除档案。
- 员工：使用 HR 建档时登记的手机号完成验证码注册，登录后提交本人任务材料并查看部门处理结果。
- 部门责任人：由 HR 在系统内创建。登录后只处理本部门任务，可确认完成或退回并要求员工补交。

员工注册前必须由 HR 先完成员工建档。演示环境会返回验证码并自动回填；生产环境请设置：

```powershell
$env:AUTH_EXPOSE_CODE = "false"
```

登录后前端使用 Bearer Token 访问受保护接口，不再通过请求头选择或声明角色。

## 7. 停止系统

分别在前后端 PowerShell 窗口按 `Ctrl+C` 停止服务。

## 8. 运行测试和生产构建

后端测试使用 H2 的 MySQL 兼容模式，不依赖业务数据库：

```powershell
mvn clean test
```

前端生产构建：

```powershell
cd frontend
npm install
npm test
npm run build
```

前端测试覆盖表单校验异常吸收、确认框取消处理和日期格式化。后端基线测试会根据 H2 测试数据覆盖认证、注册登录、任务归属和原有业务闭环。

当前自动化基线：

```text
后端：59 个测试全部通过
前端：17 个测试全部通过
生产构建：Vite 构建成功
```

## 9. 最终业务闭环

1. HR 在“任务模板”中维护任务名称、责任部门和相对入职日的截止天数。
2. HR 在“部门责任人”中为每个部门创建一个启用责任人。
3. HR 建立员工档案，系统按模板自动生成任务，并保存责任部门、原始截止日期和当前截止日期。
4. 员工登录后上传 JPG、PNG 或 PDF 材料，任务进入“待部门确认”。
5. 部门责任人查看附件并确认完成，或填写退回原因和新截止日期。
6. 已退回任务由员工重新提交；新截止日当天仍不计逾期，次日起未重交才计入逾期。
7. 所有任务确认完成后，HR 才能归档员工档案。

## 10. 常见问题

### 后端提示 Access denied

确认 `$env:DB_PASSWORD` 与当前 MySQL 的 `root` 密码一致，并确认使用的是 `3306` 端口上的目标 MySQL 实例。

### 数据库不存在或表不存在

先执行：

```powershell
$env:DB_PASSWORD = "你的 MySQL 密码"
.\scripts\init-db.ps1
```

### 前端提示网络连接失败

确认后端已启动并监听 `8080` 端口，同时检查 Vite 终端是否正常输出 `http://127.0.0.1:5173`。

### 端口被占用

- 后端可通过 `-ServerPort` 更换端口，例如：`.\scripts\run-backend.ps1 -ServerPort 8081`。
- 前端固定使用 `5173`，端口被占用时 Vite 会停止启动；请先关闭占用 `5173` 的程序，不要改用其他端口。
- 更换后端端口后，还需同步修改 `frontend/vite.config.js` 中 `/api` 的代理目标地址。
